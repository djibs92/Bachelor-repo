# ========================================
# CLOUDDIAGNOZE - CONFIGURATION ENVIRONNEMENT
# ========================================
# Copier ce fichier vers .env et remplir avec vos valeurs

# ========================================
# SÉCURITÉ JWT
# ========================================
# Clé secrète pour signer les tokens JWT
# ⚠️ IMPORTANT : Générer une clé forte et unique en production
# Exemple de génération : python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=clouddiagnoze-secret-key-change-this-in-production-2024

# Algorithme de signature JWT (ne pas modifier sauf si vous savez ce que vous faites)
JWT_ALGORITHM=HS256

# Durée de validité des tokens d'accès (en minutes)
# Par défaut : 10080 minutes = 7 jours
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ========================================
# CORS (Cross-Origin Resource Sharing)
# ========================================
# Domaines autorisés à accéder à l'API (séparés par des virgules)
# En développement : http://localhost:8000,http://127.0.0.1:8000
# En production : https://votre-domaine.com,https://www.votre-domaine.com
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# ========================================
# BASE DE DONNÉES
# ========================================
DB_HOST=localhost
DB_PORT=3306
DB_USER=clouddiagnoze_user
DB_PASSWORD=CloudDiagnoze2024!
DB_NAME=clouddiagnoze

# ========================================
# AWS CREDENTIALS (optionnel si vous utilisez AWS CLI configuré)
# ========================================
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_DEFAULT_REGION=eu-west-3

# ========================================
# APPLICATION
# ========================================
# Environnement (development, production)
ENVIRONMENT=development

# Port du serveur FastAPI
PORT=8000

# Host du serveur
HOST=0.0.0.0

